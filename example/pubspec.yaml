name: shell_route_transitions_example
description: An example project for the shell_route_transitions package.
publish_to: 'none'

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shell_route_transitions:
    path: ../
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 